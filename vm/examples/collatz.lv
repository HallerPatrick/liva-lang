


// Classical problem of crazy people
fun collatz(n)
    println(n)
    if n == 1 do
        return 1
    end
    
    if n % 2 == 0 do
        return collatz(n / 2)
    else 
        return collatz(3 * n + 1)
    end

    return
end

collatz(100)
